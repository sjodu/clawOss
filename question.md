分析一下这个 需求 测试任务：调整使得仅在环境变量里任意配置主流大模型，就可以支持任意大模型驱动项目，并复现本项目https://github.com/billion-token-one-task/ClawOSS/tree/v6-release，现状：代码写死了一些大模型配置（m2.7 kimi),导致不用这两个模型就无法启动。


验收展示：自备github账号、llm api，以及演示的服务器，演示时提交dashboard链接、已经发布一批pr的github账号，证明系统已经修复并自主运行


验收标准：可以标准化部署，并自主运行，并且可以任意配置大模型，然后正常运行提交pr到其他高质量项目，跟进可能的反馈，直到pr并得到合并


修复目标是
1。 在环境变量里面完成必要的配置，可以任意修改为主流的模型。
2。 设置一个token的总预算。跑到这么多预算的时候，服务就暂停，防止预算超限
3。dashboard 正常反映系统运行情况
4。不要通过写死流程实现目标